server:
  port: ${SERVER_PORT}
spring:
  config:
    import:
      - env.properties
  application:
    name: pointbar_application
  datasource:
    url: ${DB_URL}
    username: adminsoft
    password: dB.B0e0y!Jex>JTR1u6GyFXg3
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        show_sql: true

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: ${JWT_EXPIRATION}
